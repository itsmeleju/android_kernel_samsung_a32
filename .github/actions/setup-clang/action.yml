name: "Setup Clang & Dependencies"
description: "Installs dependencies and Clang for MTK kernel build"

runs:
  using: "composite"
  steps:
    - name: Install dependencies
      shell: bash
      run: |
        sudo apt update
        sudo apt install -y bc bison flex libssl-dev libncurses-dev libelf-dev wget git make python3

    - name: Download and Extract ZyC Clang
      shell: bash
      run: |
        if [ ! -d "clang" ]; then
          echo "[*] Downloading Clang..."
          wget -q https://github.com/ZyCromerZ/Clang/releases/download/14.0.6-20220724-release/Clang-14.0.6-20220724.tar.gz -O clang.tar.gz
          echo "[*] Extracting..."
          mkdir -p clang
          tar -xf clang.tar.gz -C clang --strip-components=1
          rm clang.tar.gz
        fi

    - name: Set Environment
      shell: bash
      run: |
        # 1. Force PATH priority for our downloaded Clang
        echo "${{ github.workspace }}/clang/bin" >> $GITHUB_PATH
        # 2. Force LLVM variables (This stops the fallback to /usr/bin/as)
        echo "ARCH=arm64" >> $GITHUB_ENV
        echo "SUBARCH=arm64" >> $GITHUB_ENV
        echo "LLVM=1" >> $GITHUB_ENV
        echo "LLVM_IAS=1" >> $GITHUB_ENV
        # 3. Define Toolchain Names
        echo "CC=clang" >> $GITHUB_ENV
        echo "LD=ld.lld" >> $GITHUB_ENV
        echo "AR=llvm-ar" >> $GITHUB_ENV
        echo "NM=llvm-nm" >> $GITHUB_ENV
        echo "OBJCOPY=llvm-objcopy" >> $GITHUB_ENV
        echo "OBJDUMP=llvm-objdump" >> $GITHUB_ENV
        echo "READELF=llvm-readelf" >> $GITHUB_ENV
        echo "STRIP=llvm-strip" >> $GITHUB_ENV
        
        # 4. Cross-Compile Prefixes (match the bins in ZyC Clang)
        echo "CROSS_COMPILE=aarch64-linux-gnu-" >> $GITHUB_ENV
        echo "CROSS_COMPILE_ARM32=arm-linux-gnueabi-" >> $GITHUB_ENV
